// 只需要输入rcc，就能自动生成下面的代码，不需要一行一行的敲。
// 组件名称和文件名一致。
// 类定义组件： rcc
// 函数定义组件：rcf
import React, { Component } from 'react'

export default class Test extends Component {
  render() {
    return (
      <div>Test</div>
    )
  }
}

